# API client (`js/api/`)

| File | Role |
|------|------|
| `apiToMongo.js` | FastAPI client — users, tests, blob upload, `MiliTestSession` identity |

Backend default: `http://127.0.0.1:8001` (see file header).
