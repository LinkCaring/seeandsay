# Changes 07.05-09.05

Brief running log for changes made from 7 May to 9 May 2026.

## 2026-05-07 to 2026-05-09

## PR title

Changelog period switch, completion summary, PLS narrative, expression feedback UX notes, app footer version 3, and inline expression AI report on completion

## Summary (2-3 bullets)
- Switched the active running changelog target to the new 7-9 May period file and froze the prior window in a dedicated 6-7 May archive.
- Reworked the completion screen (visual summary, cakes, mobile containment, Word AI export with parent-vs-AI comparison) without changing per-question 0/1/2 scoring logic.
- Delivered PLS-style narrative reporting and Word slimming (semantics-driven prompts, semantics vs category PLS clarity), recorded that optional JS traffic-sheet / evaluate-emoji sync fixes were not kept, added randomized expression "try again" voice feedback source selection, moved the start/login form into welcome step 2 while preserving the same permission/create-user flow before entering test, refined this welcome-login flow with better legal-link contrast/no nav arrows/one-time resume prompt/old-login removal/updated welcome copy, implemented explicit question end-timestamp event support so slicing windows can close on actual question transitions, switched test image resolution to prefer `.webp` with `.png` fallback for most questions while mask `A` questions keep `A.png` masks and now load option photos **PNG-first** with WebP fallback for better mask alignment, bumped the global footer label to **version 3**, show the expression AI parent-vs-AI table **inline** on the completion summary (Word download remains optional for sharing) with the inline **Match** column removed so **Reason** gets wider, persist completion AI feedback/test-id in local storage so refresh keeps the summary feedback visible, raised the expression AI daily quota default from **100** to **150**, updated the footer label again to **version 3.1**, and forced end-of-flow completion to jump straight to summary when the user answers the final CSV question while still using the existing Finish/completeSession pipeline.

## numbered main changes
1. Created `changes/CHANGES_2026-05-07_05-09.md` as the active running changelog period file.
2. Moved prior period content into `changes/CHANGES_2026-05-06_05-07.md` and stopped updating the previous 6-9 May file.
3. Added two visual summary cards on the completion screen (`Comprehension` and `Expression`) with donut breakdowns, while keeping AI status/feedback logic unchanged and rendered below.
4. Restyled and restructured the full post-navbar immediate summary block to mirror the reference (success hero card, compact overall metrics row, updated typography/spacing/colors), and moved download actions below the AI section to keep the visual summary uninterrupted.
5. Tightened vertical space and mobile portrait behavior (cards remain side-by-side), forced Rubik styling for this section, updated category labels to match evaluation popup language, and added emoji/trophy visual refinements for a more natural hero look.
6. Updated the top summary card copy/order to better match the attached design, removed extra emojis from the upper texts, and blended the trophy directly into the card background (without a separate square icon tile).
7. Added extra narrow-phone portrait breakpoints (<=480px and <=390px) with tighter spacing, smaller tiles/typography, and scaled donut/legend sizing so the upper summary fits better without excessive scrolling.
8. Fixed narrow-width hero-card alignment regression by constraining hero text width, enabling robust wrapping, and defining stable 2-column trophy/text sizing for <=480px and <=390px (including 360px width).
9. Fixed summary width consistency by enforcing border-box sizing/stretch rules on the entire immediate-summary stack (hero, stats, visual container, and visual cards), preventing inner cards from rendering wider than the main summary card on narrow portrait screens.
10. Fixed the root mobile overflow cause in the parent summary card (`session-complete`) by switching it to border-box width math and clipping overflow in both base and portrait-mobile overrides, so inner cards no longer appear wider/shifted beyond the left edge at 360px.
11. Replaced the overflow-clipping approach with direct inner-card downsizing: reduced hero/stats tile sizes and donut-card dimensions (base + narrow breakpoints) so upper and visual cards fit naturally inside the parent summary card at 360px without hiding overflow.
12. Simplified the top hero subtitle to a single short sentence (`לפניכם סיכום קצר של המשחק`), reduced stats-pill typography further, and removed the redundant “status” subtitle lines from the two donut cards (card titles remain).
13. Center-aligned the inner summary card groups by changing the donut-cards grid from full-width stretch to centered fixed-width columns across breakpoints (760/480/390), and reinforced centered placement for upper stats/visual blocks to eliminate the left-shift appearance.
14. Switched inner-summary alignment from viewport-centered margins to strict parent-card-relative layout: the immediate-summary wrapper now stretches to parent width with zero side padding, while hero/stats/visual blocks center only within that parent container.
15. Added a strict portrait-mobile containment override scoped under `.session-complete > .session-immediate-summary` so hero/stats/visual blocks are hard-bound to the parent summary card width, and reset the donut row to parent-relative 2-column `1fr` tracks (instead of viewport-shifted centering behavior).
16. Updated the portrait-mobile containment strategy to avoid navbar-like full-width behavior by switching inner summary wrapper/cards from explicit `width: 100%` to parent-relative `width: auto` with slight internal insets, and set donut-row justification to `stretch` so all inner cards stay inside the parent card bounds.
17. Added a dedicated `<400px` portrait breakpoint that rebinds the immediate-summary stack to strict `100%` parent-card width (with tighter parent padding and two equal donut columns), resolving the regression that appears specifically below 400px viewport width.
18. Added a final end-of-file `<399px` portrait “authority override” for completion summary layout (scoped to `data-test-phase="complete"` with fallback selectors) so later/mobile blocks cannot re-break containment; all inner summary blocks now inherit strict parent-card width with compact sub-400 sizing.
19. Added an additional `<410px` portrait guard band to neutralize min-content overflows (nowrap labels/legend auto columns): forced `min-width:0`, enabled wrapping on stats/legend text, and tightened legend grid tracks so content cannot push inner cards outside the parent summary container.
20. Removed the separate `<399px` authority block per request (keeping the `<410px` guard as the main mobile fix), increased trophy-text spacing in the hero, changed general-summary Hebrew copy to past tense, reordered legend rows to `color (right) -> status (middle) -> number (left)`, and added on-donut numeric badges for each pie segment.
21. Added dedicated expression cake-display variables (`expressionCakeKnewButDidntSayCount`, `expressionCakeNotThereYetCount`) and routed pie-card rendering through display-only cake stats without changing any test-flow logic that treats partial+wrong behavior as before.
22. Removed the on-donut number badges, switched donut center to a smiley emoji, moved type emojis to card-corner indicators, enforced single-line legend row alignment (number + status + color on one baseline), and expanded expression cake display to four statuses by adding a display-only `no response` segment in the pie/legend.
23. Corrected expression cake categories to match the popup’s four actual answers (`exactly`, `almost`, `knew but didn’t say`, `not there yet`) by introducing a display-only `expressionCakeCategory` per expression question (`midFailure` mapped to `knew_not_say`) while keeping test progression/skip logic unchanged (`midFailure` still recorded as `wrong` for flow decisions).
24. Added explicit in-code guards/comments clarifying that `midFailure` must continue mapping to `wrong` for adaptive flow (including consecutive-failure behavior), and adjusted card-corner emoji positioning/padding so type emojis no longer overlap summary card titles.
25. Rebuilt cake-legend row layout from CSS grid to flex alignment so each row keeps `number (left) + status (middle) + color (right)` on one line consistently across narrow widths, including the `<410px` guard band.
26. Hardened traffic-popup tap reliability by tightening option pointer/touch behavior (`touch-action`, explicit option hit-layering, contained popup scrolling) to prevent intermittent “first option not clickable until scroll” behavior on mobile.
27. Changed post-load AI feedback UX on summary: removed inline detailed AI tables/paragraphs from the screen and replaced them with a single “Download AI feedback report” action that exports the AI payload to a document file (`.txt`) when available.
28. Expanded AI feedback export actions to provide three options from the same payload: `TXT` download, Word-compatible `.doc` download (HTML-based report), and PDF print/download flow (opens a print-ready report view for Save as PDF).
29. Per request, reduced AI export options to Word-only and upgraded the Word report table to include parent answer + parent score per question, a match column, and an upfront parent-vs-AI match total (`x / y`) using the agreed mapping (`0=wrong or knew-but-didn’t-say`, `1=partial`, `2=success`).
30. Updated the Hebrew general-summary sentence in the completion card to say `והותאמה לגיל הילד` instead of `והותאמה לשלב הגיל שנבחר`.
31. Updated the Hebrew label in the general summary bullet from `שלב גיל` to `גיל`.
32. Extended backend expressive-language impression (Gemini) with PLS-oriented narrative fields, passed CSV semantics and optional category PLS into sampled clips, raised default impression token budget, normalized/padded extended fields for robust parsing, and added a completion-screen PLS narrative report UI with matching CSS (legacy impression payloads still get a shortened two-column view).
33. Clarified impression prompts and per-sample metadata so the four broad feedback buckets are explicitly driven by the **semantics** CSV column, with **category PLS** described only as supplementary fine-grained context, plus inline comments in `server.py` for maintainers.
34. Removed the aggregate AI summary JSON block from the Word expression report (per-question table and other sections unchanged at that step).
35. Stripped the Word expression report further: no generated-at/test-id/status block, no expressive-language impression JSON — only the title, parent-vs-AI match line, and per-question table (filename still uses test id).
36. Documented product decision: the expression **evaluate-emoji / traffic-sheet** optional JS reliability changes (normalize `currentIndex` before comparing in `getSafeCurrentQuestionIndex`, and gate-key-based `useEffect` so the sheet is not forced closed before the 40s window) are **not applied** in the working tree; prior open/close logic remains; current behavior is accepted for now (no overlay `pointer-events` experiment).
37. Updated expression "try again" feedback to randomly choose between `resources/questions_audio/try_again.mp3` and `resources/questions_audio/try_again2.mp3` on each playback while keeping existing replay/stop timing logic intact.
38. Moved the start/login form into welcome flow step 2 (inside the onboarding blue card), then routed to avatar screen and final welcome screen before entering test; preserved the same form validation, microphone permission gate, persistent state writes, and `createUser` call so test still starts from the microphone-check stage (no test-page navbar on this login step).
39. Refined the moved welcome-login flow: improved legal checkbox/link contrast on the blue card, hid onboarding next/previous controls while on the login screen, and removed the old login form rendering from `test.js` (fallback now routes users back to welcome if they enter test without completed welcome login state).
40. Adjusted resume/new-game UX to ask only once before entering welcome screen 2 (login): choosing resume jumps directly into the saved test state, choosing new game clears saved run and opens login; no second resume/new prompt appears later in the welcome flow.
41. Updated saved-game prompt wording/buttons to use “start new game” (`התחל משחק חדש`) instead of “open new game”, and removed the hint bullet from the final welcome “How it works” screen so it now shows only three bullets/steps.
42. Updated welcome-flow Hebrew CTAs/prompts to plural address consistently (`המשיכו מהמקום שהפסקתם`, `התחילו משחק חדש`, `התחילו משחק`) so onboarding copy speaks to caregivers in plural form.
43. Changed expression question timestamp/timer arming to start only after the initial question reading ends (matching the 40s clipping window), with a navigation fallback that marks start immediately when leaving an expression question before reading finishes so timestamp arrays never miss a start boundary.
44. Implemented explicit question-end timestamp events and windowing precedence updates: frontend now records serialized start/end events (while retaining legacy start tuples for compatibility), `test.js` marks question end on transitions and before completion, and backend timestamp parsing/window selection now prefers explicit end marks over inferred next-start fallback (still supporting legacy `[(q,t)]` payloads).
45. Fixed stale timeline carryover into fresh runs: recording restart after welcome/login now preserves question timestamps only for true in-test verification resume, while new sessions always reset timeline marks to avoid mixed old/new event streams that could produce invalid expression slice windows.
46. Hardened fresh-start isolation at mic-check continue: when `forceFreshStartAfterMicCheck` is active, the app now wipes all persisted run/timeline artifacts (timestamps, recording/session keys, score/index/result keys) and resets in-memory recorder timeline state before continuing, ensuring a genuinely fresh run from question 1.
47. Enforced fresh-run reset unconditionally on mic-check continue (not only when the fresh flag is set), since resume paths are decided before login and bypass this screen; this prevents accidental carryover into mid-game state when starting from the welcome-login flow.
48. Reduced two-image question card footprint by adding dedicated `data-count=\"2\"` image caps (including landscape/mobile overrides) so width and height scale down proportionally when only two photos are shown.
49. Improved first-question audio reliability without user fallback controls: on mic-check continue, the app now arms an immediate Q1 autoplay attempt to better preserve gesture activation, and if that attempt is blocked/timing-sensitive it automatically retries Q1 playback a few short times (with cleanup/reset when leaving question 1) instead of requiring any extra button press.
50. Added persisted parent-vs-AI comparison in backend expression payload per test (`meta.parent_ai_comparison`): rows now include parent result/score vs AI score plus match flag, alongside aggregate `match_count`, `total_compared`, and `match_rate`, with explicit pending/failed status stubs so DB records stay consistent through queue, done, and error phases.
51. Updated test image resolution order to prefer `.webp` first for question photos (including preloading paths) with `.png` fallback in UI image elements, while enforcing mask click-region assets (`A.png`) as PNG-only with no WebP fallback.
52. For mask answer type `A`, comprehension option images now resolve **`.png` first** with `.webp` on `onError`, comprehension `<img>` tags set `data-fallback-png-first`, and `ImageLoader` prefetches/`areImagesLoaded`/`prioritizeQuestion` use PNG URLs for those rows (including correct `n|m` image counts) so loading gates match displayed assets.
53. Updated the footer `app-version-label` copy in `frontend_demo/app.js` from `version 2.2` to `version 3`.
54. On session completion, render the expression AI feedback table (parent answer/score, AI score, match, reason, listen + match count) **inside** the immediate summary card below the donut cards when expression AI is resolved, sharing row logic with the Word export; added scrollable table styling in `styles.css` and clarified the Word button copy as optional download for devices that do not open `.doc` well.
55. Updated the inline completion-screen expression AI table to remove the per-row **Match** column (while keeping the top-level `x / y` match count), and expanded the **Reason** header width to use that freed space for better readability on phones.
56. Persisted `lastCompletedTestId` and `expressionAiResult` via `usePersistentState` in `test.js` so refreshing on the completion screen retains the saved AI feedback/summary state without storing large audio blobs in local storage.
57. Increased backend expression AI daily quota default by changing `GEMINI_DAILY_LIMIT` fallback in `backend/server.py` from `100` to `150` (still overridable by env var).
58. Updated the app footer label in `frontend_demo/app.js` from `version 3` to `version 3.1`.
59. Updated `test.js` end-of-question flow so answering the last active CSV question always calls `completeSession(updatedQuestionResults)` directly (same stop-recording + upload + backend flow as Finish), preventing last-question stalls behind incomplete-summary gating.

## numbered notes
1. If pre-timer evaluate-emoji opens flake again, the reverted JS guard + gate-key sync can be reintroduced narrowly without restoring full-screen dim or blocking overlay capture.

