## PR title

Early-finish dialog reuses navbar pause for recording, timer, and timestamps

## Summary (2-3 bullets)

- Opening the incomplete-test finish dialog now calls the same `pauseTest()` path as the navbar, so session recording, the session clock, and the expression 20s countdown freeze in place.
- Stay resumes via `resumeTest()` when the dialog opened the pause; finish anyway goes straight to `completeSession` without resuming; navbar pause is hidden and the generic pause overlay does not cover the dialog.
- `markQuestionEnd` is skipped while the dialog is open so timestamps stay aligned with audio slicing.

## numbered main changes

1. `test.js`: `openIncompleteSummaryConfirm`, `stayAfterIncompleteSummaryConfirm`, and `finishAnywayFromIncompleteSummaryConfirm` wire early-finish to `pauseTest()` / `resumeTest()` with `incompleteFinishDialogPausedByUsRef`.
2. `test.js`: `window.SeeAndSayTestFinishDialog.isOpen()` registered from `incompleteSummaryConfirmOpenRef` for cross-module guards.
3. `test.js`: Navbar `showPause` and `pauseDisabled` when incomplete-summary dialog is open; paused overlay only when `isPaused && !incompleteSummaryConfirmOpen`.
4. `test.js`: `pauseAwareExpressionTimer` freezes on `incompleteSummaryConfirmOpen` and does not resume the 20s countdown while the dialog is open.
5. `recording.js`: `markQuestionEnd` returns early when `SeeAndSayTestFinishDialog.isOpen()`.
6. `navbar.js`: `pauseDisabled` prop suppresses pause button rendering during early-finish dialog.

## numbered notes

1. QA on `http://localhost:8000/frontend_demo/` (backend :8001): expression Q ~12s on 20s timer → finish emoji → timer stays ~12s, recording pauses; wait on dialog → Stay → timer and recording resume; no `end` stamp at dialog-open in timestamp export.
2. QA: navbar pause not visible during dialog; manual navbar pause mid-question still freezes timer (regression).
3. QA: finish anyway → summary/upload unchanged; all questions complete → finish emoji skips dialog.
4. App version label unchanged (still 4.2 per user preference).

---

## PR title

test.js modular split — in progress (baseline + phased extract)

## Summary (2-3 bullets)

- Splitting test code into [`frontend_demo/js/`](../frontend_demo/js/) (`test/`, `app/`, `api/`, `core/`, `components/`, `session/`) per [`TEST_MODULE_MAP.md`](../frontend_demo/docs/TEST_MODULE_MAP.md) and [`STRUCTURE.md`](../frontend_demo/docs/STRUCTURE.md).
- Phase 4 (render slice): overlays, question bottom bar, and session donut summary cards use `createTestOverlays` / `createTestQuestionRender` / `createTestSummaryRender` with ctx refs bound before render; expression refresh recovery modal lives in overlays (not AI poll).
- Phases 1–8, 10, scoring extract, hint removal, full test UI render split, and `js/*` folder layout (`app`, `api`, `core`, `components`, `test`); orchestrator at `js/test/test.js` (~3k lines).

## numbered main changes

1. Added `frontend_demo/js/test/README.md` and refactor tracking in this changelog (21–22 May window).
2. Phase 0: baseline documented; module load order prepared in `index.html` as phases land.
3. Phase 1: `testAvatarVideo.js`, `testConstants.js`, `testAgeUtils.js`, `testQuestionUtils.js` — pure helpers on `window.SeeAndSayTestModules`; `test.js` thin wrappers; `index.html` loads four modules before `test.js`.
4. Phase 2: `testExpressionTimers.js` — `createExpressionTimers(getCtx)` for 20s eval / answer-end scheduling; `exprTimerCtxRef` bound before render; no behavior change.
5. Phase 3: `testMicIntro.js` — mic check, expression mic gate, intro navigation, `ensureExpressionPhaseRecording`.
6. Phase 4: `testPauseAfk.js` — pause/resume, AFK, traffic choice, early-finish dialog helpers.
7. Phase 6: `testSessionFinish.js` — `createSessionFinish(getCtx)` exports `completeSession`; `sessionFinishCtxRef` in `test.js`.
8. Phase 8 (render): `testOverlays.js`, `testQuestionRender.js`, `testSummaryRender.js` — confetti/clapping/recovery modal, bottom bar, summary donut cards.
9. Phase 5: `testQuestionFlow.js` — `loadQuestion`, `updateCurrentQuestionIndex`, `markCurrentQuestionEndTimestamp`.
10. Phase 7: `testExpressionAiPoll.js` — `refreshExpressionAiStatus`; poll `useEffect` stays in orchestrator.
11. Phase 9: `docs/DEAD_CODE_REPORT.md` — user-approved removals (~80 lines dead code in `test.js` + i18n).
12. Phase 10: `recording.js` → `js/session/*` + facade; `docs/STRUCTURE.md` for wider layout.
13. `testScoring.js` — `createTestScoring(getCtx)` for comprehension `handleClick`, mask pixel check, `finalizeComprehension*`, `playTrafficFeedback`, `handleContinue` advance/adaptive logic; `scoringCtxRef` bound before render early returns; `index.html` loads scoring before `testPauseAfk.js`.
14. `test.js` orchestrator ~3,074 lines (down from ~6,430); `testQuestionRender.js` ~606 lines; session-complete ~1,090; overlays ~480.
15. Fix: bind `exprTimerCtxRef` / `micIntroCtxRef` (and related ctx) before early returns (compr intro screen); guards in timer/mic modules when ctx not ready.
16. Phase 9 (approved): removed orphan `downloadRecording`/`downloadTimestamps`, `transcription` state, `handleLevelCompletion`, commented `formatQuestionResultsArray`; dropped i18n `test.done.download*` keys.
17. Post-stabilize QA (user): matrix spot-check all good before scoring extract.
18. Fix: `testScoring.js` `handleContinue` used bare `questionResults` (ReferenceError) — blocked confetti advance and failure advance for all comprehension types.
19. Removed in-test hints (Tiers A–D): comprehension scoring no longer uses `hintEverOpenedRef` / auto-hint / hint-capped success; partial rules unchanged (retry, wrong taps, rescue, mask 2nd tap, etc.); dropped hint state/UI (`test.js`, `testQuestionFlow`, `testQuestionRender`, `testConstants`, `test.hint.needHint`); expression traffic always shows green.
20. Ordered (2-step): two opening taps that are both not the required first image (same or different) → `failure` and advance (no rescue); e.g. answer `1→2`, taps `3` then `4` or `3` then `3`.
21. Ordered: `playTryAgainAudio` on first tap if not the required first image; on second tap if first was correct but second image is not the required second (2-up and 4-up).
22. Ordered: duplicate first after a correct first (e.g. `1→1` for answer `1→2`) plays try-again on 2nd tap; 3rd tap on the correct second image scores **partial** (rescue on 2-up, `fourUpThirdTapPartial` on 4-up).
23. `testStartScreens.js` — `tryRenderStartScreen()` for age-invalid, expression mic gate, compr/exp intro videos, preparing-recording; `startScreensCtxRef` before render early returns; `index.html` loads before `testQuestionFlow.js`.
24. `testSummaryRender.js` — `renderSessionCompleteScreen()` (upload retry, AI poll UI, PLS narrative, donut cards, Word export); `summaryRenderCtxRef` with scores, results, upload/AI state, `t`, `totalMonths`, `formatQuestionAgeBadge`, recovery helpers; `test.js` delegates via `ensureTestSummaryRender()`.
25. `testOverlays.js` — paused overlay, AFK warning, expression traffic popup, incomplete-summary dialog; `overlaysCtxRef` extended (`tr`, pause/resume, traffic handlers, `questions` + `getCurrentQuestionIndex`); ~270 lines removed from orchestrator main return.
26. `testQuestionRender.js` — `renderQuestionSection()` (query row, comprehension/expression image grids, expected-answer note, bottom actions); `questionRenderCtxRef` bound after layout vars (`currentIdx`, `images`, click state, handlers); orchestrator main return ~370 lines slimmer.

## numbered notes

1. QA baseline: matrix items 1–9 on `http://localhost:8000/frontend_demo/` + backend `:8001` (manual; app label `version 4.1` in `app.js` at refactor start).
2. Phase 4 QA: streak confetti/clapping; expression bottom bar + 20s countdown; session-complete donut cards; refresh recovery modal mid-expression.
3. Phase 7 QA: full test → summary expression AI poll (2s then 5s); manual Refresh.
4. Phase 10 QA: mic intro starts recording; navbar pause/resume; finish session MP3 upload; 12:30 cap; early-finish dialog still blocks `markQuestionEnd`.
5. Paused overlay, AFK, traffic popup, and incomplete-summary dialog now in `testOverlays.js` (item 25).
6. Phase 9 dead-code removed per user approval (see item 16 in main changes); `isMobile` kept (still used).
7. After scoring extract: QA comprehension single/multi/ordered/mask, expression traffic sheet, 3-in-row celebration, adaptive skip-to-expression / end-on-two-wrongs.
8. After session-complete extract: full test → summary stats/donuts, upload failure retry, recover-saved-test, expression AI pending/refresh/failed, PLS report + Word download when AI done.
9. After overlays extract: navbar pause/resume overlay; AFK popup; expression traffic sheet (4 options + examples); early-finish dialog (Stay / Finish) with timer freeze unchanged.
10. After question-section extract: image grids (2/3/4/5-up, two-row), replay audio, age badge, green borders on comprehension taps, expression eval bar at bottom.
11. Fix: `testQuestionRender.js` `renderImage` used bare `clickedMultiAnswers` / `orderedAnswers` / `orderedClickSequence` (ReferenceError on comprehension render); build script lookahead updated for `.includes` / `.length`.
12. `testQuestionRender.js` — `renderTestNavbar()` and `renderQuestionLoadingScreen()`; `questionRenderCtxRef` set before early returns; orchestrator ~3,050 lines.
13. Removed unused in-test hint CSS from `test.css` (chest, trigger, bottom-bar hint rules); welcome/onboarding hint copy unchanged.
14. Frontend folder layout: `js/app/` (`app.js`, `welcome.js`), `js/api/`, `js/core/`, `js/components/`, `js/test/test.js`; `index.html` load paths updated; `docs/STRUCTURE.md` rewritten.
15. Per-folder `README.md` under `js/app`, `js/api`, `js/core`, `js/components`; build tools point at `js/test/test.js`.

---

## PR title

Welcome split, docs under changes/, remove blob browser smoke

## Summary (2-3 bullets)

- Welcome flow split into `welcomeShared.js`, `welcomeLogin.js`, `welcomeScreens.js`, and a thin `welcome.js` orchestrator; `index.html` load order updated with no intended behavior change.
- Historical frontend Markdown (`FRONTEND_DEMO_CHANGELOG`, UI PR notes, backend gap list) moved to `changes/docs/`.
- Removed `blob-smoke-test.html` and config generation; Phase A blob check remains CLI-only via `backend/tools/blob_smoke_phase_a.py`.

## numbered main changes

1. `welcomeShared.js` — `SeeAndSayWelcomeModules`: i18n, localStorage, resume/tips modals; resume delegates to `SeeAndSayTestRun`.
2. `welcomeLogin.js` — child profile, legal consent, mic gate, `createUser`, advance to intro video.
3. `welcomeScreens.js` — screen1 intro, intro video, screen3 how-it-works + start game CTA.
4. `welcome.js` — orchestrator (~200 lines): nav dots, video autoplay effects, delegates screen bodies to modules.
5. `index.html` — loads `welcomeShared.js` → `welcomeLogin.js` → `welcomeScreens.js` → `welcome.js` after `app.js`.
6. Moved `FRONTEND_DEMO_CHANGELOG.md`, `NEW_UI_CHANGES_2026-04-17.md`, `PR_UI_SUMMARY.md`, `LOGIN_START_BACKEND_NOTES.md` to `changes/docs/`.
7. Deleted `frontend_demo/blob-smoke-test.html`; dropped `blob-smoke-config.local.json` from `.gitignore`.
8. `blob_smoke_phase_a.py` — server PUT + CORS preflight only; no browser config file.
9. Removed unused `frontend_demo/bottomnav.js` (demo never loaded it).
10. Deleted leftover `blob-smoke-config.local.json` from `frontend_demo/`.
11. Removed legacy `frontend/` and `frontend_not_updated_ignore/` trees (deploy uses `frontend_demo/` only; user kept local zip backups).
12. Removed empty `frontend_demo/js/recording/` directory (recording code lives in `js/record_session/` + root `recording.js`).
13. Renamed `js/session/` → `js/record_session/`; updated `index.html` and `recording.js` comment.
14. Reorganized `js/test/` into `utils/`, `flow/`, `scoring/`, `ui/`, `finish/` (orchestrator stays `test.js`); load order unchanged in `index.html`.
15. Added `frontend_demo/README.md` — full structure, app flow, `test.js` orchestrator role, backend API map, and why `recording.js` stays at demo root.
16. User-facing product name set to **MILI** in `index.html` title, `i18n` logo alt text, and `frontend_demo` docs; repo path and `SeeAndSay*` / `seeandsay*` code identifiers unchanged.
17. Rewrote `frontend_demo/README.md` for new contributors: TOC, flow-first layout, component connection diagram, per-folder file roles, concise test/backend sections; deep detail stays in `docs/`.
18. Renamed JS globals to `Mili*` (`MiliTestModules`, `MiliTestRun`, `MiliTestSession`, `MiliWelcomeModules`, `MiliRecordingParts`, `MiliTestFinishDialog`, `MILI_EXPRESSION_ANSWER_MS`, `MILI_API_BASE_URL`); left `seeandsay*` storage keys and production backend hostname unchanged.

## numbered notes

1. User smoke-tested app flow OK before this pass; re-check welcome → login → intro video → how-it-works → test after pull.
2. Onboarding traffic preview copy updated to “Partial” (no hint wording) on screen3; tips modal still mentions hints (modal commented out on screen3).
3. `changes/CHANGES_2026-05-16_16.md` item 127 still references removed browser smoke page (historical).

---

## PR title

Backend audit docs (dead code + structure)

## Summary (2-3 bullets)

- Added read-only backend audit under `backend/docs/`: dead-code report with frontend→API matrix and approval checklist, plus recommended future folder layout.
- Documented unused routes (`VerifySpeaker`), orphan files (`testings.py`, `TTS_Google.py`, `Explaintion`), and trim candidates in `MongoDB.py` / `AI_Models_API.py` — no runtime or deletion changes.
- `backend/docs/README.md` indexes the two reports; elimination and physical moves wait on checklist approval in the dead-code report.

## numbered main changes

1. `backend/docs/BACKEND_DEAD_CODE_REPORT.md` — API route inventory vs `apiToMongo.js`, whole-file classification, `MongoDB`/`AI_Models_API` symbol usage, legacy `audioFile64` note, master approval table.
2. `backend/docs/BACKEND_STRUCTURE_RECOMMENDATION.md` — proposed `app/`, `api/routes/`, `services/`, `storage/`, `ai/`, `tools/` layout with per-file migration map and phased migration steps.
3. `backend/docs/README.md` — pointer to audit docs and local run hint.

## numbered notes

1. Production hostname and storage key names unchanged; repo name unchanged.
2. Phase B/C deletions and file moves are optional follow-ups only after approving items in the dead-code report.

---

## PR title

Backend Tier 1 dead-code cleanup

## Summary (2-3 bullets)

- Removed orphan backend files and unused MongoDB helpers; trimmed dead models/imports in `server.py` and commented OpenAI stubs in `AI_Models_API.py` with no change to live demo API routes.
- Moved offline TTS script to `backend/tools/tts_generate_audio.py`; kept `score_expression_with_gemini`, `VerifySpeaker`, and `speechmatics-python` for Tier 2.
- `import server` smoke check passes; run full welcome→finish→summary QA before deploy.

## numbered main changes

1. Deleted `backend/testings.py` and `backend/Explaintion`.
2. Moved `TTS_Google.py` → `backend/tools/tts_generate_audio.py`.
3. `server.py` — removed `UploadFile` import, `GetFinalTranscriptionRequest`, commented `getUser` route block, stale `get_user_config` comment in `create_user`.
4. `MongoDB.py` — removed unused methods (`get_user_config`, `get_latest_test`, admin/debug helpers, `get_user_audioFile_from_64base`, `create_storage`) and unused GridFS setup.
5. `AI_Models_API.py` — removed commented `openai_whisper_runner` / `openai_llm_runner` blocks; kept `score_expression_with_gemini` and OpenAI client.

## numbered notes

1. Tier 2 not done: `POST /api/VerifySpeaker`, Speechmatics chain, `speechmatics-python` in requirements.
2. `audioFile64` path unchanged.

---

## PR title

Backend Tier 2 + module map docs

## Summary (2-3 bullets)

- Removed unused `POST /api/VerifySpeaker`, Speechmatics/speaker-verification code, and `speechmatics-python`; replaced `import *` with explicit Gemini/audio imports in `server.py`.
- Added `backend/docs/BACKEND_MODULE_MAP.md` and `BACKEND_STRUCTURE.md` listing every backend file, live routes, and request flow (mirrors frontend_demo docs).
- Kept `audioFile64` and `score_expression_with_gemini`; dev scripts stay under `tools/` only.

## numbered main changes

1. `server.py` — deleted VerifySpeaker route/model; explicit imports from `AI_Models_API`.
2. `AI_Models_API.py` — removed `speechmatics_runner_from_bytes`, `speaker_recognition`, `speaker_verification`.
3. `requirements.txt` — removed `speechmatics-python`.
4. `backend/docs/BACKEND_MODULE_MAP.md` — file inventory, Mermaid flow, route table, `server.py` regions.
5. `backend/docs/BACKEND_STRUCTURE.md` — short layout + rules; updated `docs/README.md` index.

## numbered notes

1. Redeploy Render after pull so requirements match (no Speechmatics on server).
2. Optional `.env` cleanup: `SPEECHMATICS_API_KEY` no longer used.
3. Future optional removal: `audioFile64` path only after blob-only frontend + no other API clients.

---

## PR title

Remove backend tools folder

## Summary (2-3 bullets)

- Deleted `backend/tools/` (`blob_smoke_phase_a.py`, `tts_generate_audio.py`) — CLI-only dev scripts, not used by the API or MILI demo.
- Updated `BACKEND_MODULE_MAP.md` and `BACKEND_STRUCTURE.md` so the backend tree lists only runtime-relevant files.
- Azure/TTS debugging can be done via a full test finish flow or git history if those scripts are needed again.

## numbered main changes

1. Removed `backend/tools/blob_smoke_phase_a.py` and `backend/tools/tts_generate_audio.py`.
2. `backend/docs/BACKEND_MODULE_MAP.md` — dropped tools section; noted removal in “not present” list.
3. `backend/docs/BACKEND_STRUCTURE.md` — removed `tools/` from layout and dev-script rule.

## numbered notes

1. Historical changelog entries still mention blob smoke CLI (e.g. 2026-05-16 item 127); use git to recover scripts if needed.
2. No server restart required beyond normal deploy; no dependency changes.

---

## PR title

Backend docs tidy + root README rewrite

## Summary (2-3 bullets)

- `backend/docs/` now has two active guides (module map + structure) plus index; dead-code audit and future-split proposal moved to `changes/docs/` as archived references.
- Root `README.md` rewritten: repo map, architecture, local quick start, and doc index for both `frontend_demo` and `backend`.
- Product naming clarified as MILI while noting legacy repo/host/storage identifiers.

## numbered main changes

1. Moved `BACKEND_DEAD_CODE_REPORT.md` and `BACKEND_STRUCTURE_RECOMMENDATION.md` to `changes/docs/` with archived banners.
2. Updated `backend/docs/README.md`, `BACKEND_STRUCTURE.md`, `BACKEND_MODULE_MAP.md` for the slimmer docs layout.
3. Replaced root `README.md` (removed outdated VerifySpeaker/Speechmatics/TTS sections; added mermaid flow and folder goals).

## numbered notes

1. Dashboard and seeandsay-resources repo remain external to this tree.
2. `frontend_demo/README.md` stays the deep dive for the UI; root README is the macro entry point.

---

## PR title

Bump app footer version to 4.2

## Summary (2-3 bullets)

- Bottom footer label now shows version 4.2 for the current demo build.

## numbered main changes

1. Updated `app-version-label` text in `frontend_demo/js/app/app.js` from 4.1 to 4.2.

## numbered notes

1. None.

---

## PR title

Persist API session ids + recording interruption banner

## Summary (2-3 bullets)

- `seeandsayTempBackendUserId`, `seeandsayPendingTestId`, and blob-upload flags now persist in `localStorage` (with one-time migration from `sessionStorage`) so reloads, calls, and iPad tab discard keep the same backend user and test run.
- Session recording monitors mic stream health (`track.onended`, resume failures); users see a dismissible banner when audio may be incomplete, including after returning to a visible tab.
- `test.js` wires the banner and visibility health checks; upload recovery reads pending test id from `MiliTestSession`; README persistence note updated.

## numbered main changes

1. `apiToMongo.js` — `readStoredApiKey` / `writeStoredApiKey` with session→local migration; temp backend user id, pending test id, and upload keys use localStorage; exported `clearTempBackendUserId` and `ensurePendingTestId`.
2. `app.js` — full reset clears localStorage API keys plus legacy sessionStorage copies.
3. `recordingCapture.js` + `recording.js` — stream health monitor, `checkRecordingHealth`, `setOnRecordingInterrupted`, and interrupted-state helpers.
4. `testOverlays.js` + `i18n.js` — recording-interrupted alert banner (he/en).
5. `test.js` — banner state, interrupted callback, visibility/resume health checks, overlay ctx; `autoRecoverAfterUploadFailure` uses `ensurePendingTestId()`.
6. `testSessionFinish.js` — blob-upload progress flag written to localStorage.
7. `frontend_demo/README.md` — persistence section documents localStorage API keys.

## numbered notes

1. QA: login → refresh mid-test → finish should attach results to the same Mongo user (no empty `tests[]` from userId mismatch).
2. QA: simulate mic loss (OS call or revoke permission) during expression — banner appears; dismiss works; finish/upload still attempts with available chunks.
3. Does not restart recording after interrupt; IndexedDB chunk backup remains a future option.

---

## PR title

Bump app footer version to 4.3

## Summary (2-3 bullets)

- Bottom footer label now shows version 4.3 for the current demo build.

## numbered main changes

1. Updated `app-version-label` text in `frontend_demo/js/app/app.js` from 4.2 to 4.3.

## numbered notes

1. None.

---

## PR title

Store clientInfo on each finished test for debugging

## Summary (2-3 bullets)

- Each `addTestToUser` payload includes `clientInfo` (browser UA, screen, app version, recording/upload flags, API vs demo user ids) for correlating device-specific failures in Mongo.
- Backend persists `clientInfo` on the test document and logs a short summary line on Render.
- Exposed `MiliTestSession.collectClientInfo` for manual inspection in the console.

## numbered main changes

1. `apiToMongo.js` — `collectClientInfo()` and attach `clientInfo` in `updateUserTests`.
2. `server.py` — optional `clientInfo` on `AddTestRequest`; structured log on finish.
3. `MongoDB.py` — save `clientInfo` on each pushed test record.

## numbered notes

1. `clientInfo` is for internal debugging only; not shown in the parent UI.
2. After deploy, finish one test and confirm `tests[].clientInfo` in Mongo and the Render `clientInfo` log line.

---

## PR title

Bump app footer version to 5

## Summary (2-3 bullets)

- Footer and `clientInfo.appVersion` now report version 5 for the current demo build.

## numbered main changes

1. Updated `app-version-label` in `frontend_demo/js/app/app.js` from 4.3 to 5.
2. Updated `MILI_APP_VERSION` in `frontend_demo/js/api/apiToMongo.js` to match.

## numbered notes

1. None.
