# Changes 14.05–15.05

Brief running log for changes made from **14 May to 15 May 2026** (active append target for expression-only recording / incremental AI work and related notes).

## 2026-05-14 to 2026-05-15

## PR title

Expression-only incremental audio: plan, first full-stack slice (draft / clips / finalize)

## Summary (2-3 bullets)

- **Docs & plan:** Expression-only incremental-audio plan under **`docs/plans/`** (glossary, **30s** clip semantics, resume/refresh, execution todos including **client auto-stop**, and observability for Gemini, caps, and troubleshooting).
- **Backend:** Draft test + **`expressionClip`** + **`finalizeTest`** on **`server.py`** / **`MongoDB.py`**, **30s** clip trim aligned to **`GEMINI_MAX_SEGMENT_SECONDS`**, monotonic finalize progress, legacy **`addTestToUser`** defaults, richer **INFO** logging for clips and Gemini, and expression rubrics/impression metadata aligned to CSV **`category_PLS`** / **`sub_category_PLS`** (legacy **`semantics`** / **`category PLS`** still read when present).
- **Frontend:** Session timeline and draft flow without comprehension **`MediaRecorder`** buffering; expression clips with **pause-aware 30s active-time client cap**, worker **MP3** path, **`postExpressionClipWithRetry`** + **`runWithExpressionClipConcurrency`** + **`finalizeUserTestsWithRetry`**, **`sessionGoHomeBlock`** overlays, summary **expressionAi** polling/progress fixes, handoff yields before encode, **try_again** feedback without random alternates, and **expression traffic countdown frozen** during streak clapping.
- **Impression:** One Gemini call now includes **all comprehension** results (grouped correct/partly/wrong with **`category_PLS`** / **`sub_category_PLS`** from server CSV) plus up to **10** expression audio samples — no extra per-question comprehension API calls.

## numbered main changes

1. Created [`docs/plans/README.md`](docs/plans/README.md) listing plan subfolders and linking the expression-only optimization plan.
2. Created [`docs/plans/expression-only-incremental-audio/README.md`](docs/plans/expression-only-incremental-audio/README.md) and [`docs/plans/expression-only-incremental-audio/PLAN.md`](docs/plans/expression-only-incremental-audio/PLAN.md) documenting phases, sequence diagram, risks, file touch list, and execution todos for the optimization.
3. Added this file [`changes/CHANGES_2026-05-14_15-05.md`](changes/CHANGES_2026-05-14_15-05.md) as the active dated changelog for 14–15 May 2026 (filename encodes **14** and **15** in `YYYY-MM-DD` form: `2026-05-14` … `2026-05-15`).
4. Updated [`docs/plans/expression-only-incremental-audio/PLAN.md`](docs/plans/expression-only-incremental-audio/PLAN.md) with agreed refinements: **30s-capped** clip window from end of initial reading (popup delay excluded from audio), **strict no session recording during comprehension**, upload-vs-score backlog strategy, and **summary-screen progress** fields (`processed` / `remaining` / phase).
5. Further updated [`docs/plans/expression-only-incremental-audio/PLAN.md`](docs/plans/expression-only-incremental-audio/PLAN.md): glossary (BSON, base64, GridFS), **default inline clips** (GridFS optional), **document-only** legacy branching (no device-based behavior), and **resume vs refresh** requirements including listen/timing parity on expression resume.
6. Added PLAN sections **Backward compatibility and operations** (no manual Mongo edits for rollout; optional migration is separate) and **Per-question audio** (inline array default for free tier; GridFS/blob id only as fallback when document size warrants).
7. Tuned PLAN for **bounded expression volume (~35 × ≤30s)**: de-emphasized 16 MB as a day-one risk; **inline-only** expected on free tier; GridFS documented as unlikely rare fallback; aligned glossary and risks table.
8. Corrected product constant in PLAN and changelog summary from **~25** to **~35** expression questions (each clip still **≤ ~30 s**).
9. Extended [`backend/MongoDB.py`](backend/MongoDB.py) with **`get_user_test_by_id`**, **`add_draft_test_to_user`**, **`append_expression_audio_clip`**, and **`finalize_draft_test`** for in-progress tests storing **`expressionAudioClips`** alongside existing fields.
10. Extended [`backend/server.py`](backend/server.py) with clip trim/score helpers, **`_run_expression_clip_score_background`**, **`_run_finalize_expression_ai_from_clips_background`**, and routes **`POST /api/createTestDraft`**, **`POST /api/tests/{test_id}/expressionClip`**, **`POST /api/tests/{test_id}/finalizeTest`** while keeping legacy **`POST /api/addTestToUser`** unchanged.
11. Updated [`frontend_demo/recording.js`](frontend_demo/recording.js) with **`initSessionTimelineClock`**, **`startExpressionClipRecording`**, **`stopExpressionClipRecording`**, **`isExpressionClipRecording`**, pause/resume/cleanup integration for the expression recorder, and **`isRecordingActive`** including expression clip state.
12. Updated [`frontend_demo/apiToMongo.js`](frontend_demo/apiToMongo.js) with **`createTestDraft`**, **`postExpressionClip`**, and **`finalizeUserTests`** calling the new backend endpoints.
13. Updated [`frontend_demo/test.js`](frontend_demo/test.js) to start the question timeline after the reading step (and in the direct post-age flow), **`createTestDraft`**, arm **expression clip** recording, **upload clips** on traffic submit, **`finalizeUserTests`** on session complete when a draft exists, and helpers **`trafficResultToHeadlight`** / **`readBlobAsDataURL`**.
14. Removed the **download session MP3** button and **`downloadRecording`** handler from the session-completed UI in [`frontend_demo/test.js`](frontend_demo/test.js); timestamps download remains.
15. Guarded **`startSessionRecordingDirectFlow`** so it does not re-run timeline + **`createTestDraft`** when **`sessionRecordingStarted`** is already true after the reading handoff.
16. Fixed expression traffic UX regression in [`frontend_demo/test.js`](frontend_demo/test.js): **`handleTrafficPopupChoice`** no longer waits on MP3 encode + **`readAsDataURL`** + **`postExpressionClip`** before **`handleContinue`** (upload continues in the background; promises remain in **`clipUploadPromisesRef`** for finalize).
17. Summary-screen expression AI **progress text** now uses **`countAnsweredByType(..., "expression")`** as the denominator while **`status === "pending"`** when the session answered fewer expression items than the CSV-wide draft total, so counts like **1/36** no longer appear for a short partial run; added phase labels for **`scoring_clips`**, **`finalize_merge`**, and **`draft`**.
18. [`backend/server.py`](backend/server.py) **`post_expression_clip`** now runs **`_store_trimmed_expression_clip_data_url`** so **`expressionAudioClips`** in Mongo store **at most the first `GEMINI_MAX_SEGMENT_SECONDS`** (default 30s) of audio per upload, matching scoring caps and preventing long post-popup tails from bloating documents.
19. [`backend/server.py`](backend/server.py) **`AddTestRequest`** defaults **`audioFile64`** to **`""`** and **`timestamps`** to **`"{}"`** so legacy **`addTestToUser`** validates when the client has no session recording blob.
20. [`frontend_demo/apiToMongo.js`](frontend_demo/apiToMongo.js) **`updateUserTests`** sends empty audio and **`{}`** timestamps when arguments are null/empty; **`createTestDraft`** logs a hint when fetch fails; **`postExpressionClip`** logs URL, question id, headlight, and audio payload size per request.
21. [`frontend_demo/test.js`](frontend_demo/test.js) **`completeSession`** enters the monolithic recording poll only when **`SessionRecorder.isMediaRecorderLive()`** is true; otherwise it **`updateUserTests`** with **`""`** plus **`SessionRecorder.getTimestampText()`** (or **`{}`**), and **`console.warn`** when mic is on but **`draftTestId`** is missing.
22. [`docs/plans/expression-only-incremental-audio/PLAN.md`](docs/plans/expression-only-incremental-audio/PLAN.md) execution todos extended with optional **30s client recorder stop**, **draft/API-down UX** (no false MP3-wait spinner), and **DevTools clip visibility** expectations.
23. [`docs/plans/expression-only-incremental-audio/PLAN.md`](docs/plans/expression-only-incremental-audio/PLAN.md) **Known issues and observability** section documents **main-thread MP3 stutter** after clip end, **finalize vs incremental Gemini** timing, **progress counter regression** (and server fix), **stored duration vs cap**, and checklist when feedback is missing despite a clip POST.
24. [`backend/server.py`](backend/server.py): **`_run_finalize_expression_ai_from_clips_background`** emits **`processed_questions`** from **`count_scored_rows`** so UI progress does not jump backward; **`FinalizeDraftTestRequest.timestamps`** defaults to **`"{}"`**; startup **INFO** logs **`GEMINI_MAX_SEGMENT_SECONDS`**; **`expressionClip`** logs stored byte length and cap; **INFO** logs before **Gemini** per-question score and before finalize **impression**; clip scoring prompt uses the configured cap seconds.
25. [`frontend_demo/apiToMongo.js`](frontend_demo/apiToMongo.js): **`finalizeUserTests`** sends **`"{}"`** when timestamps are empty, logs **`finalizeTest`** error bodies on failure, and logs **`expression_ai.status`** / phase on success.
26. [`frontend_demo/test.js`](frontend_demo/test.js): **`console.error`** when **`finalizeUserTests`** returns **null** after Finish so missing **`lastCompletedTestId`** / polling is visible.
27. [`backend/server.py`](backend/server.py): **`GEMINI_MAX_SEGMENT_SECONDS`** read from env is **clamped to at least 30** (with **`logging.warning`** if a lower value was set) so Mongo **`trimmed_max_seconds`** and audio trim match the **30s** expression UI; startup log reflects the effective value.
28. [`frontend_demo/test.js`](frontend_demo/test.js): **`completeSession`** no longer clears **`lastCompletedTestId`** before draft finalize; it sets **`lastCompletedTestId`** to **`draftTestId`** immediately so the summary can poll **`expressionAiStatus`** during **`Promise.allSettled`** on clip uploads; polling runs when **`expression_ai`** is **null** or **pending**, fires an **immediate** refresh, and repeats every **2s**; **`refreshExpressionAiStatus`** only treats a **string** override as a test id (avoids click **event** misuse).
29. [`docs/plans/expression-only-incremental-audio/PLAN.md`](docs/plans/expression-only-incremental-audio/PLAN.md): observability notes updated for **min-30 cap** and **post-Finish polling** before **`finalizeTest`** returns.
30. [`frontend_demo/i18n.js`](frontend_demo/i18n.js) and [`frontend_demo/test.js`](frontend_demo/test.js): **`sessionGoHomeBlock`** (`draftStart` | `clipSave`) fullscreen overlay when **`createTestDraft`** fails after the question timeline is live and the mic is on, or when **`postExpressionClip`** fails mid-test (simple “return home” copy, single CTA); **`resumeTestSessionRecordingAfterReading`** always attempts **`createTestDraft`** (even when falling back to continuous recording), clears the scheduled Q1 **`markQuestionStart`** timeout on draft failure, and keeps **`markQuestionStart`** tied to **`timelineOk`**; direct post-age flow skips Q1 mark when draft creation fails; AFK overlay hidden while blocked; [`docs/plans/expression-only-incremental-audio/PLAN.md`](docs/plans/expression-only-incremental-audio/PLAN.md) execution todo **11** and **Known issues** updated for draft-failure UX and idle-yield mitigation.
31. [`frontend_demo/recording.js`](frontend_demo/recording.js): **`yieldBeforeMp3Encode()`** (`requestIdleCallback` with timeout, else **`setTimeout(0)`**) before **`convertToMP3`** on expression-clip **`onstop`** and continuous session **`onstop`** to reduce main-thread contention with the next question’s audio.
32. [`frontend_demo/apiToMongo.js`](frontend_demo/apiToMongo.js): **`postExpressionClip`** returns **`{ ok: true, … }`** or **`{ ok: false, status, networkError? }`** and logs short HTTP error bodies on failure; [`frontend_demo/test.js`](frontend_demo/test.js) keeps **`draftTestIdRef`** in sync for async uploads, clears **`draftTestId`** when a clip save fails, and shows **`sessionClipSaveFailed`** i18n for **`clipSave`**; [`frontend_demo/recording.js`](frontend_demo/recording.js) adds **`yieldForQuestionAudioHandoff()`** (double **`requestAnimationFrame`** + short idle) before expression-clip MP3 conversion so the next question’s **`<audio>`** can start before heavy encode work.
33. Added [`frontend_demo/mp3-encode-worker.js`](frontend_demo/mp3-encode-worker.js): first iteration attempted **decode** inside the worker (blocked where **`AudioContext`** is missing in workers); superseded by item **34** (**`encodePcm`** only).
34. [`frontend_demo/mp3-encode-worker.js`](frontend_demo/mp3-encode-worker.js) and [`frontend_demo/recording.js`](frontend_demo/recording.js): worker **`encodePcm`** path (**lamejs** only) after **main-thread** **`decodeAudioData`** + mono mix — fixes **“No AudioContext in worker”** on browsers without worker **`AudioContext`**; main-thread fallback shares **`decodeBlobToMonoFloat32`** + **`encodeMonoFloat32ToMp3OnMain`** and uses **channel-averaged mono** like the worker.
35. [`frontend_demo/apiToMongo.js`](frontend_demo/apiToMongo.js): **`runWithExpressionClipConcurrency`** wraps **`postExpressionClip`** (default **max 2** in-flight POSTs, override via **`window.SEEANDSAY_EXPRESSION_CLIP_MAX_PARALLEL`** up to **8**); **`postExpressionClipWithRetry`** unchanged contract; **`finalizeUserTestsCore`**, **`finalizeUserTestsWithRetry`** (default **3** attempts, exponential backoff capped at **8s**, same transient HTTP codes as clips + **`networkError`**), and **`finalizeUserTests`** delegating to **`finalizeUserTestsCore`**; [`frontend_demo/test.js`](frontend_demo/test.js) draft **`completeSession`** path calls **`finalizeUserTestsWithRetry`** after **`Promise.allSettled`** on clip uploads; [`docs/plans/expression-only-incremental-audio/PLAN.md`](docs/plans/expression-only-incremental-audio/PLAN.md) execution todo **4** client slice marked **v1 complete** (optional stronger backpressure later).
36. [`frontend_demo/recording.js`](frontend_demo/recording.js): **pause-aware** expression-clip **active-time** auto-stop (default **30s**, **`window.SEEANDSAY_EXPRESSION_CLIP_MAX_SECONDS`** clamped **5–120**) so encoding stops when the server would trim; **`stopExpressionClipRecording`** returns a **single shared promise** while a stop is in flight (idempotent with the timer and traffic).
37. [`frontend_demo/test.js`](frontend_demo/test.js): **try-again** wrong-answer feedback always plays **`resources/questions_audio/try_again.mp3`** (removed random pick and **`try_again2.mp3`** asset).
38. [`frontend_demo/test.js`](frontend_demo/test.js): **Expression traffic countdown** pauses while the **streak clapping** overlay is visible (deadline frozen like session pause; resumes after overlay unless the session is paused).
39. [`backend/server.py`](backend/server.py): Expression rubrics read **`category_PLS`** as the broad PLS frame and **`sub_category_PLS`** as supplementary detail, with fallbacks to legacy **`semantics`** and **`category PLS`** / **`category_pls`**; impression **`linguistic_goal_line`** and per-sample **`pls_semantics_area`** / **`pls_category`** use the same broad-vs-supplementary roles (stored JSON keys unchanged).
40. [`backend/prompts.py`](backend/prompts.py) and [`backend/AI_Models_API.py`](backend/AI_Models_API.py): Expressive-language impression instructions and per-sample Hebrew metadata lines refer to **`category_PLS`** and **`sub_category_PLS`** instead of **`semantics`** / **`category PLS`**.
41. [`.cursor/skills/expression-scoring-pipeline/SKILL.md`](.cursor/skills/expression-scoring-pipeline/SKILL.md): Pipeline doc updated for the renamed CSV columns.
42. [`backend/server.py`](backend/server.py): Loads **הבנה** rubrics from CSV; **`_build_comprehension_impression_entries`** joins **`full_array`** comprehension tuples with **`category_PLS`** / **`sub_category_PLS`**; **`_run_expressive_language_impression`** passes them into the single impression call (shared by finalize and legacy paths).
43. [`backend/prompts.py`](backend/prompts.py): **`build_comprehension_impression_block`** formats comprehension items grouped by **correct / partly / wrong** with PLS columns per line; impression instructions mention the comprehension block.
44. [`backend/AI_Models_API.py`](backend/AI_Models_API.py): **`summarize_expressive_language_impression_gemini`** accepts **`comprehension_entries`** and prepends the text block before expression audio samples.
45. [`backend/server.py`](backend/server.py): **`finalize_merge`** progress counts background clip scores not yet in **`rows_out`** and merges **`per_question`** snapshots so UI no longer drops (e.g. **13/36** → **1/36**) at finalize start.
46. [`frontend_demo/test.js`](frontend_demo/test.js): Summary expression-AI progress uses a monotonic ref so displayed **processed** never decreases between polls.
47. [`frontend_demo/recording.js`](frontend_demo/recording.js): Cache expression clip blob after **30s auto-stop** so traffic submit can still upload (fixes **`missing_clip_at_finalize`** when the popup opens after the cap).
48. [`backend/server.py`](backend/server.py) + [`frontend_demo/test.js`](frontend_demo/test.js): **`finalize_merge`** logs missing clip question numbers; client warns when stop returns no blob for a question.
49. [`frontend_demo/recording.js`](frontend_demo/recording.js): Traffic submit after **30s cap** now **awaits in-flight `expressionClipStopPromise`** (MP3 encode) instead of returning null; **`startExpressionClipRecording`** no longer clears cached blob or starts a second recorder while waiting for traffic.
50. [`frontend_demo/recording.js`](frontend_demo/recording.js): Expression clip **recording** auto-stop uses **30s policy + 800ms grace** (override `window.SEEANDSAY_EXPRESSION_CLIP_RECORDING_GRACE_MS`); UI timer and server trim stay at **30s** so stored MP3 duration aligns with the on-screen countdown.
51. [`frontend_demo/app.js`](frontend_demo/app.js): Footer **`app-version-label`** updated from **version 3.2** to **version 3.3**.
52. [`frontend_demo/test.js`](frontend_demo/test.js): Expression popup **30s timer** and **traffic-open timeout** now **pause and resume** together (clear/reschedule enable timer on pause; full remaining time after resume).
53. [`frontend_demo/recording.js`](frontend_demo/recording.js) + [`frontend_demo/test.js`](frontend_demo/test.js): Expression **clip active-time cap** freezes on test pause/clapping, aligns to **UI seconds left + grace** on resume; test pause always calls **`pauseRecording`** (draft clip-only path); auto-stop no longer fires while recorder is **paused**.

## numbered notes

1. Prior period entries remain in [`changes/CHANGES_2026-05-07_05-09.md`](changes/CHANGES_2026-05-07_05-09.md); append new work for 14–15 May here unless the calendar window rolls again.
2. If the team prefers the literal label “14–15.5” on disk, rename or add an alias file; current name follows existing `CHANGES_YYYY-MM-DD_MM-DD.md` convention.
3. **`POST /api/addTestToUser`** remains for backward compatibility; if **`createTestDraft`** fails, the client uses the legacy monolithic upload path **only** when a **live** continuous session **`MediaRecorder`** exists; otherwise it posts results + timestamps only (no clip AI without a draft).
4. Mid-test voice re-verify, Finish while an expression clip is still open, and strict upload-queue backpressure are not fully exercised in this slice; **`$push`** clip history allows multiple blobs per question (server uses the **last** clip per **`question_number`** for finalize).
5. **`POST /api/createUser` 400** on welcome usually means the temp **`userId`** already exists in Mongo (benign for demos); finishing mid-expression without traffic logs **discarded unsent expression clip** because that clip has no headlight payload to upload.
6. One **`expressionClip`** console line per answered expression requires a successful **`createTestDraft`** first; if the backend is down at session start, expect **no** per-question clip logs and no expression AI from clips for that run.
7. **Gemini** work is visible in **uvicorn** logs: **`expressionClip accepted`**, **`Gemini expression score`**, **`Gemini expressive_language_impression (finalize_merge)`**; absence usually means quota skip, decode failure, or no rubric — not that the client skipped the HTTP call.
8. If the question timeline fails to start but **`createTestDraft`** succeeds, the tab can still hold a **`draftTestId`** without timeline marks — rare; the go-home overlay applies when the timeline **did** start and the draft then fails.
9. **`yieldBeforeMp3Encode`** and **worker `encodePcm`** (lame off the main thread when supported) are best-effort; **`decodeAudioData`** still runs on the main thread before PCM is posted to the worker.
10. **`POST …/expressionClip` 404** means Mongo did not find **`userId` + `tests.testId`** (e.g. DB reset while the tab still holds an old **`draftTestId`**, or draft never stored); finalize then emits **`missing_clip_at_finalize`** for that question. **`ERR_CONNECTION_REFUSED`** is the server or proxy not accepting connections mid-upload.
11. **`mp3-encode-worker.js`** needs to be served from the same origin as the app (not `file://` without special flags) and must be allowed to **`importScripts`** the lame CDN URL; strict **CSP** may block the worker or the script load — use **`window.SEEANDSAY_MP3_WORKER = false`** to force the legacy main-thread encoder.
12. **`postExpressionClipWithRetry`** does not retry **404** or other non-retryable **4xx**; repeated failures add backoff delay before the go-home **`clipSave`** path runs.
13. **`finalizeUserTestsWithRetry`** uses the same transient-retry policy as clips; **`finalizeUserTests`** (non-draft callers) stays single-attempt via **`finalizeUserTestsCore`**.
14. Expression clip **client cap** defaults to **30s** of **active** recording (elapsed time while **recording** reduces the remaining budget; pause does not extend the window); override **`window.SEEANDSAY_EXPRESSION_CLIP_MAX_SECONDS`** only for QA and keep it aligned with **`GEMINI_MAX_SEGMENT_SECONDS`** when testing trim parity.