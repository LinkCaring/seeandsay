# Changes 16.05

Brief running log for changes on **16 May 2026** (responsive CSS refactor + avatar test flow).

## 2026-05-16

## PR title

16 May — responsive layout refactor, avatar intros, and expression mic gate

## Summary (2-3 bullets)

- **Portrait-only responsive refactor:** CSS split into `colors.css`, `layout.css`, and `screens/*.css` with tier breakpoints for golden **390×705**, sm tall **412×915**, lg tablet **768×1024**, short-wide **1280×800** / compact **1168×632**, and tall desktop **1920×1080**; progress bar removed; `usePhoneLikeGrid` drives phone-like question grids on golden, short-wide, and tall desktop.
- **Test flow with avatar intros:** welcome → comprehension intro (WebM or **`compr_intro_fallback.mp4`**) → comprehension questions; after last comprehension → sound check → expression intro (WebM or **`exp_intro_fallback.mp4`**) → expression questions with existing recording/traffic logic.
- **Audio and gates:** question/try-again playback stops when entering sound check or summary; intro videos autoplay on entry; first comprehension and first expression prompts use immediate-read mic-gate paths after each intro ends.

## numbered main changes

1. Added `changes/CHANGES_2026-05-16_16.md` and updated `.cursor/rules/pr-output-structure.mdc` for the 16 May changelog window.
2. Added `.cursor/skills/seeandsay-responsive-layout/SKILL.md` with tier table, golden viewport rule, file map, and `@media` inventory.
3. Extended `frontend_demo/css/colors.css` with tier CSS variables (`--tier-golden-max`, `--tier-content-max`, etc.).
4. Created `frontend_demo/css/layout.css` with app shell; imported at top of `styles.css`.
5. Removed landscape `@media` blocks and rotate-overlay CSS/JS (portrait-only).
6. Created `screens/welcome.css` and `screens/onboarding.css`; extracted landing and onboarding rules.
7. Created `screens/test.css`; extracted test-flow rules (question section, images, traffic, mic-check, navbar, progress, session complete, comprehension/expression grids) from `styles.css`.
8. Added `@import "./screens/test.css"` after onboarding in `styles.css`; hub retains shared globals and pre-test screens (age, voice).
9. Added `@media (min-width: 411px)` in `test.css` for `--tier-content-max` width cap and `container-type: inline-size` on `.question-section` / `.images-container`.
10. Fixed 390×705 under-fill: onboarding tall portrait frame `display: flex` + `100dvh` calcs; golden `700–710px` fill in `layout.css` and `test.css`; test `480px` padding limited to `[data-page="test"]`.
11. Phase 4: merged safe duplicate `@media` in `test.css` (768px, 600px+portrait dev/bottom-actions, 480px pls), `onboarding.css` (tall portrait 650px ×2), and hub `styles.css` (voice 600px); **47 → 44** width/feature queries.
12. Phase 4: wider tiers on `welcome.css` (`411px` content cap, `720px` two-column cards) and expanded `test.css` `411px`/`720px` question shell + multi-column image grids.
13. Phase 4: tier-purpose file headers on `colors.css`, `layout.css`, `styles.css` hub, and all `screens/*.css`; fixed orphaned `test.css` comment opener before responsive display section.
14. `colors.css`: `--tier-desktop-max` / wider `--tier-content-max` at `min-width: 1024px` for 1280×800 band.
15. `welcome.css` + `onboarding.css`: `1024px` horizontal layouts (screen-1 two-column, steps grid), reduced section `min-height` forcing scroll.
16. `test.js` + `navbar.js`: removed bunny/carrot from `ProgressBar`; hide `test-navbar__lang` on test variant.
17. `test.css`: `.progress-track*` replaces bunny track; `721px` + `max-height: 860px` caps for `images-container--single` / `data-count="1"`.
18. `test.css`: Dropped `700–710px` golden flex-fill on question `.page-content`; track-only progress bar; full fixed navbar at `min-width: 601px`; `720px` grid expansion gated to `min-height: 861px`.
19. `test.js`: Render `ProgressBar` on portrait mobile again (slim track only).
20. `welcome.css` + `onboarding.css`: `1024px` + `max-height: 860px` short-wide tuning; screen-1 stacks; login `.age-screen` controls enlarged.
21. `styles.css`: Base `.age-screen` inputs/checkboxes slightly larger for golden phone.
22. `onboarding.css`: `1024px` + `max-height: 860px` — centered S1 emoji bullets, larger illustration, smaller centered S2 login card.
23. `test.js`: `usePhoneLikeGrid` (`≤600px portrait` or `721px` + `max-height: 860px`) drives three-up / two-col / five-up markup; removed `ProgressBar` render.
24. `test.css`: Mirrored portrait phone grid rules under `721px` + `max-height: 860px`; removed progress-track / progress-bar / ctrl-btn CSS; questions `.page-content` uses navbar offset only.
25. `navbar.js`: Test variant omits LinkCaring logo; `test.css` hides `.test-navbar__logo` at `min-width: 601px`.
26. `test.css` (`721px` + `max-height: 860px`): Smaller choice-image caps, tighter gaps/padding on question shell and multi-row grids so full question view fits ~800px without scroll; golden ≤410 untouched.
27. `test.css` (`721px` + `max-height: 860px`): Raised short-wide image caps; six-choice grids switch to 3×2; `padding-top` + `margin-top` add space below fixed test navbar before query row; golden ≤410 untouched.
28. `test.css` (`721px` + `max-height: 860px`): Square choice tiles on wrappers (`aspect-ratio: 1`, flex-centered images); removed height-only `max-height` caps; tokens `--short-wide-choice-max` (`34vh`/`45cqw`/`260px`), `--short-wide-choice-max-3col`, `--short-wide-images-block-max` `68vh`; golden ≤410 untouched.
29. `test.css` (`721px` + `max-height: 860px`): Raised single-image caps — `--short-wide-single-block-max` `min(calc(100dvh - 260px), 540px)`, `--short-wide-single-max` `min(62vh, 56cqw, …)`; square wrapper + `object-fit: contain`; golden ≤410 untouched.
30. `test.css` (`721px` + `max-height: 860px`): Session-complete / summary uses `min(900px, 92vw)` card, two-column stats + visual donuts, larger typography, wider PLS narrative columns; golden ≤410 untouched.
31. `test.css` (`721px` + `max-height: 860px`): Summary screen vertical fill — reduced outer/card padding, `min-height: calc(100dvh - …)` on complete card, flex `1fr` summary row, larger hero/stats/donuts (`--short-wide-summary-donut` ~168px); golden ≤410 untouched.
32. `test.css` (`721px` + `max-height: 860px` + `[data-test-phase="complete"]`): Narrower overview column — hero + `.session-immediate-summary__stats` (מבט כללי) capped with `--short-wide-summary-overview-max`; donuts span rows 1–2 at `1.42fr`, `--short-wide-summary-donut` ~184px; golden ≤410 untouched. *(Reverted in #33.)*
33. `test.css` (`721px` + `max-height: 860px` + `[data-test-phase="complete"]`): **Undo #32** — restore full-width hero and `1fr`/`1.12fr` stats|donuts grid (`--short-wide-summary-donut` ~168px); מבט כללי `.session-immediate-summary__stats-grid` **2×2** with square tiles, tighter stats padding, compact hero; golden ≤410 untouched.
34. Phase 5: merged three `test.css` `max-width: 600px` blocks (navbar, notes, age/mic margins) and two `min-width: 601px` blocks (dev-mode + fixed navbar); merged hub `styles.css` duplicate `600px` voice rules into early mobile block (kept before `768px` for cascade).
35. Phase 5: `test.js` `isMobile` breakpoint `640px` → `600px` to match CSS phone band; `minImgWidth` still unused in render — no layout change at 390×705 or 1280×800.
36. Phase 5: updated `.cursor/skills/seeandsay-responsive-layout/SKILL.md` — file map **48** `@media`, short-wide band, Phase 5 merge table, deferred height tiers; dead `progress-track` / landscape already absent.
37. Phase 6: `welcome.css` — sm `411–479px` portrait padding bridge; lg tablet `768–1023px` portrait content cap + two-column cards (not short-wide density).
38. Phase 6: `onboarding.css` — sm flow padding; lg tablet centered flow, single-column S1, taller illustration slot, single-column S2 steps.
39. Phase 6: `test.css` — sm auto-fit/image caps; lg tablet `data-count` 2–3 col grids + square tiles and session-complete width cap (overrides tall `720×861` at tablet only).
40. Phase 6: skipped merge of three scattered `600px portrait` blocks in `test.css` (cascade order vs ~600 lines between blocks).
41. Phase 6: responsive-layout skill — phases 0–6 marked complete, QA matrix, **54** `@media` inventory, “refactor complete” deferred list (golden height tiers skipped).
42. `layout.css`: sm tall `411–479` + `min-height: 880px` — `.landing-wrapper` / `.onboarding-flow` viewport stretch (mirrors golden fill; ≤410 untouched).
43. `welcome.css`: sm tall — `.landing-section` padding-block `clamp(40px, 5.5vh, 72px)`, intro/start section vh padding, `.landing-section__inner` gap `clamp(20px, 3vh, 32px)`.
44. `onboarding.css`: sm tall — `.onboarding-flow` / `.onboarding-frame` flex-fill + `gap`/`padding` clamps; `.onboarding-screen` / `.onboarding-nav` vertical rhythm.
45. `test.css`: sm tall — questions `.page-content` gap `16px` + `padding-top: calc(var(--test-navbar-height) + 10px)`; `.question-section` `margin-top: 16px` / `padding-top: 10px`; query-row `margin-bottom: 22px`; containers `gap: 18px` / `margin-top: 10px`; image cells `clamp(136px, 42vw, 212px)`; grid gaps `18px`.
46. `test.css`: sm tall — `[data-test-phase="complete"]` min-height stretch, session-complete padding, donuts `--sm-tall-summary-donut` ~108px, hero/stats/visual rhythm (golden + welcome untouched).
47. `test.css`: lg tablet — single-image `--lg-tablet-single-max` `min(62vh, 72cqw, 560px)` + block max `calc(100dvh - 220px)`; questions `padding-top: calc(var(--test-navbar-height) - 14px)` (~42px).
48. `test.css`: lg tablet — session-complete 2-col summary fill, `--lg-tablet-summary-donut` ~200px, `gap: 14px 28px` between stats and donuts; mic phase flex-centered, meter `min(560px, 94%)` × `clamp(32px, 4vh, 40px)`, larger type/buttons.
49. `test.css` sm tall `411–479` + `min-height: 880px` + `[data-test-phase="complete"]`: hero `flex-direction: column`, `padding-top: 18px`, trophy `44px` — fixes overlap with “well done”.
50. `test.css` sm tall complete: `.session-immediate-summary__stats-grid` `repeat(2, 1fr)` + square `aspect-ratio: 1` tiles (not 4-across).
51. `test.css` sm tall questions: image/row gaps `18px` → `23px`; query-row `margin-bottom` `28px`; container `margin-top` `14px`.
52. `test.css` lg tablet questions: `.page-content` `padding-top: calc(var(--test-navbar-height) - 20px)` (was `-14px`).
53. `test.css` lg tablet complete: summary grid `0.88fr` / `1.22fr`; stats column narrower padding; donut column wider.
54. `test.css` lg tablet complete: `.session-visual-summary__donut` `aspect-ratio: 1`, matching max width/height, `flex-shrink: 0`; chart-wrap `flex: 0 0 auto` — circles not ellipses.
55. `test.css` lg tablet complete: summary grid `minmax(200px, 0.75fr)` / `minmax(0, 1.25fr)`; מבט כללי `.session-immediate-summary__stats-grid` single column (`1fr`, 4 rows); stat tiles no longer square.
56. `test.css` lg tablet complete: donut cards `justify-items: center`; chart-wrap fixed square (`--lg-tablet-summary-donut`); donut fills wrap at `100%` + `aspect-ratio: 1`; visual-summary `gap: 20px`.
57. `test.css` sm tall complete: `.session-immediate-summary__stat-label` `13px` (was base `10px` at 412px).
58. `test.css` sm tall questions: image/grid gaps `23px` → `28px`; `--two-col` `row-gap`/`column-gap` `28px`; `--three-up-bottom` `margin-top: 6px`.
59. `test.css` sm tall complete: מבט כללי `__stat-label` `15px`, `__stat-value` `24px`, `__stat-value--small` `16px`, `__stats-title` `18px` (was `13px` label only; values inherited `14px` from `480px`).
60. `test.css` lg tablet complete: summary `grid-template-columns: 1fr` stacked layout — stats `grid-column: 1 / -1` (4-row column), donuts `1 / -1` with internal `repeat(2, 1fr)`; removed `0.75fr`/`1.25fr` side-by-side split; `row-gap: 20px` between sections.
61. `test.css` sm tall + lg tablet complete: donut legend `__legend-row` / `__legend-label` `13px` (412) and `14px` (768); lg tablet only `__stats-title` `26px`, `__stat-label` `20px` (golden + short-wide untouched).
62. Post-closure: `.cursor/skills/seeandsay-responsive-layout/SKILL.md` — `index.html` → `colors.css` + hub `@import` chain; QA sign-off checklist for all four user-approved viewports; dead-CSS grep notes; md `480–767` bridge documented as skipped.
63. Post-closure: grep verified no `rotate-overlay`, `progress-track`, or `ProgressBar` in `frontend_demo/`; `orientation: landscape` zero; session-complete bunny-carrot art retained.
64. `index.html`: `viewport-fit=cover` on viewport meta so `env(safe-area-inset-top)` applies on notched phones.
65. `styles.css`: `@media (max-width: 600px) portrait` — `.top-header` `padding-top: max(8px, env(safe-area-inset-top))`, `padding-bottom: 8px`, logo `40px`; removed duplicate `520px` logo rule.
66. `colors.css`: `--app-header-height: 57px` at `max-width: 600px` for shell calcs.
67. `welcome.css`: `[data-page="home"]` `padding-top: 0` (was `36px` from hub `992px`/`520px`); landing-nav `top` uses `--app-header-height`; dropped `480px` logo override (hub `600px` owns it).
68. `layout.css`: sm tall `411–479` + `min-height: 880px` — `.landing-wrapper` `min-height: 0` (was `calc(100dvh - header)`).
69. `onboarding.css`: `max-width: 600px` portrait — `min-height: auto` on flow/frame, compact S1 card + illustration caps; golden `≤410×700–710` block re-applies flex fill; sm tall drops forced `100dvh` min-heights on flow/frame.
70. Tall laptop browser (`min-width: 900px` + `min-height: 861px`): `colors.css` `--app-header-height` 62px; `styles.css` compact `.top-header` (46px logo, `overflow: visible`).
71. Tall laptop browser: `layout.css` / `welcome.css` / `onboarding.css` — drop forced viewport min-heights, cap S1 illustration, single-column S1; sticky nav uses `--app-header-height`; golden ≤410, sm-tall 412×915, short-wide 1280×800 untouched.
72. Short-wide compact `721×≤700`: `colors.css` `--app-header-height` 58px; `styles.css` compact top-header (46px logo, 6px padding).
73. Short-wide compact: `layout.css` — `min-height: 0` on `.landing-wrapper` / `.onboarding-flow` (no forced stretch).
74. Short-wide compact: `welcome.css` — tighter section padding, `min-height: auto`, compact sticky nav offset.
75. Short-wide compact: `onboarding.css` — flex frame, smaller S1 illustration/card, fits ~632px below header.
76. Short-wide compact: `test.css` — lower `--short-wide-*` caps, navbar/query/photo spacing, stacked complete summary (no `100dvh` min-heights), compact mic phase; `721×860` block untouched.
77. `.cursor/skills/seeandsay-responsive-layout/SKILL.md` — short-wide compact tier, **1168×632** QA row, **60** `@media` inventory.
78. `onboarding.css` (`721×≤700`): larger S1 illustration slot (`24–36vh`, image up to `860px` wide / `34vh`); second compact block after global `.age-screen` — S2 login card `440px`, `40px` fields, `8px` gaps, `12–13px` legal copy (overrides base `52px` inputs at 1168×632).
79. `test.css` (`721×≤700`): raised compact `--short-wide-choice-max` `32vh`/`43cqw`/`245px`, `--short-wide-choice-max-3col` `30vh`/`30cqw`/`205px`, `--short-wide-single-block-max` `calc(100dvh - 268px)`/`470px`, `--short-wide-single-max` `56vh`/`52cqw`, `--short-wide-images-block-max` `58vh`/`500px`; `721×860` untouched.
80. Tall desktop `900×861`: `colors.css` `--tier-content-max` / `--tier-desktop-max` `min(1100px, 94vw)`; `styles.css` header logo **48px**.
81. Tall desktop: `welcome.css` — 2-col intro, capped sections, `1280×861` 3-col cards; `onboarding.css` — 2-col S1, large illustration, S2 login card **520px** without scroll.
82. Tall desktop: `test.css` — gate moved from `720×861` to `900×861`; wide square tiles, compact navbar, balanced query/photo spacing, 2-col summary with circular donuts **~220px**.
83. `.cursor/skills/seeandsay-responsive-layout/SKILL.md` — tall desktop tier table + **1920×1080** QA row.
84. `onboarding.css` (`721×≤700`): S1 illustration slot `30–44vh` / `200–300px`, image up to `980px` × `42vh`; slightly tighter unified card padding for balance.
85. `test.css` (`721×≤700`): questions `padding-top` `navbar + 14px`, `.question-section` `margin-top: 8px`; complete summary — `flex: 0` shell, hero `40px` icon / `20px` title, non-square 2×2 stats, `--short-wide-summary-donut` `92px`/`14.5vh`/`108px`; `721×860` untouched.
86. `colors.css` (`900×861` / `1280×861`): `--tier-content-max` / `--tier-desktop-max` `min(1280px, 96vw)` → `min(1400px, 96vw)` at `1280×861+` (was `1100px`/`94vw`).
87. `test.js`: `usePhoneLikeGrid` also true for `(min-width: 900px) and (min-height: 861px)` — tall desktop uses same `--two-col` / `--three-up` / `--five-up` markup as golden/short-wide.
88. `test.css` (`900×861`): removed 3–4 col `data-count` grids; phone-like flex/grid rules + raised caps `--tall-desktop-choice-max` `44vh`/`32cqw`/`360px`, `--tall-desktop-images-block-max` `calc(100dvh - 240px)`/`82vh`/`860px`; six-choice stays 2×3 (no short-wide 3×2 override).
89. `welcome.css` (`900×861` / `1280×861`): full-width sections, wider padding, larger intro type; `1280×861` 3-col `.landing-how-grid`; golden + short-wide untouched.
90. `onboarding.css` (`900×861`): flow width follows `--tier-content-max`, larger S1 illustration (`42–54vh` slot, image up to `520px`/`50vh`), wider 2-col gap.
91. `layout.css` (`900×861`): home/onboarding `.page-content` full width (no hub side padding); `.landing-wrapper` / `.onboarding-flow` `width: 100%`.
92. `test.css` (`721×≤700`): complete summary — `min(900px, 92vw)` card, **side-by-side** stats (`1fr`) | donuts (`1.12fr`), `--short-wide-summary-donut` `130px`/`20.5vh`/`140px`, hero `52px`/`26px`, square stat tiles; questions — **34px** row gap on `.images-container`, `--two-col`, `--three-up`/`--five-up` (8px within-row); image caps unchanged; `721×860` untouched.
93. `welcome.css` (`900×861`): sections `width: 100%` / `max-width: min(1400px, 96vw)`, tighter side padding, larger `.landing-title` (**40–60px**) and intro copy; card/how padding and type scaled up; `1280×861` 3-col cards unchanged.
94. `onboarding.css` (`900×861`): flow/frame capped at **1400px** / **96vw**; S1 illustration slot **440–520px** (**48vh**), image up to **500px**; frame `min-height: min(640px, 78vh)` (no whole-page `100dvh` scroll); S2 login card **680px**, larger fields/type.
95. `layout.css` + `test.css` (`900×861`): mic phase `.page-content` flex-centered `min-height: calc(100dvh - test-navbar)`; mic card **`min(800px, 90vw)`**, meter **`min(680px, 92%)` × `36–44px`**, title **32–40px**, body **18–22px**.
96. `test.js`: `comprIntroVideoComplete` persistent gate, `compIntro` test phase, autoplay/retry effects, and early-return screen for `resources/avatar/compr_intro.webm`.
97. `test.js`: blocks `loadQuestion` and question-audio autoplay until comprehension intro completes; arms first-question mic gate on video end.
98. `test.css` + `layout.css`: centered full-viewport styles for `data-test-phase="compIntro"` (mirrors mic intro layout).
99. `test.js`: `awaitingExpressionMicCheck` gate + `tryGateExpressionMicCheckBeforeNavigatingTo` on advance to first הבעה; `continueFromExpressionMicCheck` no longer starts test at question one.
100. `test.js`: autoplay and `loadQuestion` allow comprehension audio before mic check; refresh resumes expression mic gate when index is already in expression phase.
101. `welcome.js` / `app.js`: reset `awaitingExpressionMicCheck`, `comprIntroVideoComplete`, `expIntroVideoComplete`, and `pendingExpressionIntroIndex` on new login and test-run clear.
102. `test.js`: `stopAllQuestionPlayback()` — pause/clear question + try-again audio, cancel first-question retry timers; clears try-again `onended`.
103. `test.js`: call `stopAllQuestionPlayback` on expression mic gate, `awaitingExpressionMicCheck` effect, pause, and session complete.
104. `test.js`: guards on `playQuestionAudio`, `playTryAgainAudio`, autoplay effect, and first-question retry scheduler during mic gate, expression intro, and summary.
105. `test.js`: `expIntroVideoComplete`, `beginExpressionIntroBeforeIndex`, `finishExpressionIntroVideo`; mic continue shows intro before first expression index.
106. `test.js`: first-expression autoplay uses `firstQuestionMicGateArmed` like Q1; blocks question load/autoplay until expression intro completes.
107. `test.css` + `layout.css`: `expIntro` phase shares centered avatar intro layout with `compIntro`.
108. `test.js`: `canPlayWebmWithTransparency()` + `resolveAvatarIntroVideoSources` — iOS / no-WebM devices use `compr_intro_fallback.mp4` and `exp_intro_fallback.mp4` instead of `.webm`.
109. `test.js`: `onError` swaps to MP4 fallback if WebM fails to load; `test-avatar-intro__video--solid-bg` drops drop-shadow on opaque fallback.

## numbered notes

1. **User sign-off (16 May):** **390×705**, **1280×800**, **412×915**, **768×1024** — approved; golden and short-wide `721×860` blocks must stay untouched on future edits.
2. **1168×632 QA:** Verify fullscreen **1168×632** — test questions (34px between photo lines), session summary (side-by-side stats|donuts, ~130px donuts); re-check DevTools **1280×800** unchanged (`max-height: 700` excludes height 800).
3. **Tall desktop QA:** Verify fullscreen **1920×1080** — welcome intro width, onboarding S1 illustration + S2 login **680px**, mic card/meter size; test questions + summary; re-check **1280×800** and **1168×632** unchanged.
4. **Real-phone retest:** Hard refresh after deploy; confirm logo + onboarding S1 at **360×800**, **390×844**, **412×915**.
5. **Responsive refactor complete** — deferred by design: height tiers `max-height:699` / `min-height:711` at ≤410px; three `600px portrait` merges; two `480px` test blocks; md `480–767` dedicated tier; `usePhoneLikeGrid` extended to tall desktop `900×861` (lg tablet still CSS `data-count` only).
6. **Avatar flow QA:** welcome → `compr_intro` → Q1 comp audio; last comp → mic screen (audio stops) → `exp_intro` → first expression audio; hard refresh mid-test on Q2+ should not replay comprehension intro; refresh on expression before mic should return to mic screen.
7. **Avatar autoplay:** If blocked, first tap anywhere retries intro video (same pattern as welcome `intro1.webm`).
8. **Intro fallback QA:** On iPhone or a browser without WebM, confirm `*_fallback.mp4` plays for comprehension and expression intros (white baked background, no shadow halo).
8. Lang toggle omitted on test variant only (`navbar.js`); home/complete nav unchanged.
